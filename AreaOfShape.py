# Take user inputs for length, width, and height
length = float(input('Length? '))
width = float(input('Width? '))
height = float(input('Height? '))

# Calculate the volume
answer = length * width * height

# Print the result
print('The volume is:', answer)
